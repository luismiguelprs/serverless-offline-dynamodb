{
  "name": "serverless-offline-dynamodb",
  "version": "0.0.12",
  "description": "",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "/dist"
  ],
  "scripts": {
    "build": "tsc",
    "prepublish": "npm run build"
  },
  "repository": "luismiguelprs/serverless-offline-dynamodb",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.2.3",
    "@types/serverless": "^3.12.11",
    "serverless-offline-dynamodb": "^0.0.7",
    "typescript": "^5.0.4"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.338.0",
    "@aws-sdk/util-dynamodb": "^3.338.0"
  },
  "peerDependencies": {
    "serverless": "^3.31.0"
  }
}
